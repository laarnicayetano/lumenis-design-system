The only input treatment in the system: caps label, underline-only field.

```jsx
<TextField label="Work email" type="email" required value={email} onChange={e => setEmail(e.target.value)} />
<TextField label="How can we help?" multiline rows={4} />
```

Use `tone="inverse"` inside black panels and footers. Never add a filled box or rounded corners.
