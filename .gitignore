# Licensed fonts — never redistribute. See README.md "Fonts" for where to
# place local copies; tokens/fonts.css falls back gracefully without them.
assets/fonts/
*.otf
*.ttf
*.woff
*.woff2

# Raw source material — not system material. Distilled into tokens/ and
# guidelines/ instead; keep the originals in Drive, not in a public repo.
uploads/
research/
*.pdf

.DS_Store
node_modules/

# Build output — regenerated by `npm run build` / CI. Never version controlled.
dist/
*.tsbuildinfo
.build-tmp/

.playwright-mcp/

TODO.md