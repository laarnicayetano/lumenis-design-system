# Licensed fonts — never redistribute. See README.md "Fonts" for where to
# place local copies; tokens/fonts.css falls back gracefully without them.
assets/fonts/
*.otf
*.ttf
*.woff
*.woff2

# Raw source material — not system material. Distilled into tokens/ and
# guidelines/ instead.
uploads/
research/
*.pdf

.DS_Store
node_modules/
input/

# Build output — regenerated by `npm run build` / CI. Never version controlled.
dist/
.build-tmp/

.playwright-mcp/

TODO.md

_ds_bundle.js
_ds_manifest.json
_adherence.oxlintrc.json
*storybook.log
storybook-static

# Generated Storybook docs pages mirroring guidelines/ui_kits — regenerated
# by scripts/build-storybook-content.ts before `storybook`/
# `build-storybook` run.
stories/generated/
stories/

# One-off codemods (see .claude/skills/code-mods/SKILL.md) — already run,
# kept locally for reference
scripts/migrations/

# Finished one-off outputs (e.g.  presentations from the design-slides
# skill) — not reusable templates
output/

# design-sync (claude.ai/design) — build artifact, transient scratch,
# verification working state, staged scripts. Never version controlled.
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/
